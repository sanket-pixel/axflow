preprocessing:
  resize_mode: stretch
  normalize: false
  input_is_bgr: true

inference:
  model_dir: yolov8s/yolov8s_coco_compiled
  num_cores: 1

postamble:
  enabled: true
  graph_path: postprocess_graph.onnx